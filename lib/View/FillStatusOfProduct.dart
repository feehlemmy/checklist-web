import 'package:flutter/material.dart';

class FillStatusOfProduct extends StatelessWidget {
  const FillStatusOfProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
